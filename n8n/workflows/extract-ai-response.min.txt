const geminiResponse = $input.first().json;
const _parts = geminiResponse.candidates?.[0]?.content?.parts || [];

let aiText = '';
for (let i = _parts.length - 1; i >= 0; i--) {
  const tx = _parts[i] && _parts[i].text ? _parts[i].text.trim() : '';
  if (tx) { aiText = tx; break; }
}
aiText = aiText.replace(/^\s*(think|thought|reasoning)\b[\s\S]*?\n\n/i, '').trim();

let emptyReason = '';
if (!aiText) {
  const cand = (geminiResponse.candidates || [])[0] || {};
  emptyReason = String(cand.finishReason || '')
    || String((geminiResponse.promptFeedback || {}).blockReason || '')
    || 'no_text_part';
  const safetyHit = ((cand.safetyRatings || []).filter(r => r && r.blocked) || [])
    .map(r => r.category).join(',');
  console.log('[Empty Reply] model returned no text | finishReason=' + emptyReason
    + (safetyHit ? ' | blockedCategories=' + safetyHit : '')
    + ' | parts=' + _parts.length
    + ' | promptTokens=' + ((geminiResponse.usageMetadata || {}).promptTokenCount || '?'));

  aiText = 'ji, bataiye - aapko kya problem ho rahi hai aur kab se? Main aapki help karti hu.';
}

const buildData = $('Build AI Prompt').first().json || {};
let phone = buildData.phone;
if (!phone) { try { phone = $('Decide Process').first().json.phone; } catch (e) {} }
if (!phone) throw new Error('Extract AI Response: phone missing. buildData=' + JSON.stringify(buildData));
const name = buildData.name || 'Customer';
const newMsgText = buildData.newMsgText || '';

const _askedIdentity = /\b(who\s*(are|r)\s*(you|u)|who\s*made\s*(you|u)|kaun\s*ho|kon\s*ho|kaun\s*hai|kon\s*hai|tum\s*kaun|tum\s*kon|aap\s*kaun|aap\s*kon|kisne\s*banaya|kisne\s*bnaya|introduce\s*your|apna\s*introduction)\b/i
  .test(String(newMsgText || ''));

let _lastOutText = '';
try {
  const _outs = $('Fetch Conversation History').all().map(i => i && i.json)
    .filter(m => m && m.direction === 'out' && m.text)
    .sort((a, b) => (Number(a.msgTime) || 0) - (Number(b.msgTime) || 0));
  if (_outs.length) _lastOutText = String(_outs[_outs.length - 1].text || '');
} catch (e) {}

let greetedBefore = false;
try {
  const items = $('Fetch Conversation History').all();
  for (const it of items) {
    const m = it && it.json; if (!m) continue;
    const txt = (m.text || '').toLowerCase();

    if (m.direction === 'out' && m.text && String(m._type || '') !== 'BOT_PLACEHOLDER') greetedBefore = true;
    if (txt.includes('ananya') && txt.includes('sehat')) greetedBefore = true;
  }
} catch (e) {}

function sanitizeReply(t) {
  if (!t) return '';
  let s = String(t);
  s = s.replace(/\b(good\s*(morning|afternoon|evening|noon)|gud\s*(mrng|morning|evening|evng)|shubh\s*(prabhat|sandhya)|subh\s*prabhat)\b[\s,!.-]*/gi, '');

  s = s.replace(/\b(ma['\u2019`]?am|m['\u2019`]am|mam|madam|mdm|mem(\s*(saab|sahab|saheb|sahib))?)(\s*(ji|jee))?\b/gi, '');

  s = s.replace(/\bsirs?(\s*(ji|jee))?\b(?!\s*(dard|dukh|dukhna|pain|ache|ghum|ghoom|chakkar|bhari|heavy|me\b|mein\b))/gi, '');
  s = s.replace(/\b(bhai|bhaiya|bhaiyya|bhaisahab|bro|brother|behen|bahen|behn|didi|bhabhi)(\s*(ji|jee))?\b/gi, '');

  s = s.replace(/(\u092E\u0948\u0921\u092E|\u092E\u0948\u092E|\u092E\u0947\u092E|\u092E\u0947\u092E\u0938\u093E\u0939\u092C|\u0938\u0930\s*\u091C\u0940|\u0938\u0930\u091C\u0940|\u092D\u0948\u092F\u093E|\u092D\u093E\u0908\u0938\u093E\u0939\u092C|\u092D\u093E\u0908\s*\u0938\u093E\u0939\u092C|\u0926\u0940\u0926\u0940|\u092C\u0939\u0928\s*\u091C\u0940)/g, '');

  s = s.replace(/^[\s>*-]*(ananya|agent|bot|assistant)\s*(ji)?\s*[:\-\u2013\u2014]+\s*/gim, '');
  s = s.replace(/hello\s*ji[\s,!.]*/gi, '');

  if (!_askedIdentity) {

    const QR_INTRO_SENT = /(\bananya\b[^.!?\n]{0,40}\b(baat\s*kar|bol\s*rah|hu\b|hoon\b)|\bsehat\s*up\s*se\b[^.!?\n]{0,30}\b(baat\s*kar|bol\s*rah)|\b(this\s+is|i\s*am|i'?m)\s+a+n+[ay]+n*[ya]*\b|\ba+n+[ay]+n*[ya]*\b[^.!?\n]{0,30}\b(health\s*expert|health\s*advisor|team\s*sehat\s*up|from\s*sehat\s*up|at\s*sehat\s*up|sehat\s*up\s*se)\b)/i;
    s = s.split(/(?<=[.!?])\s+|\n+/).filter(x => !QR_INTRO_SENT.test(x)).join(' ');
    s = s.replace(/\bma?i?n?\s*ananya\s+baat\s+kar\s+rah[iy]\s*hu?n?\s*,?\s*(sehat\s*up\s*se)?[\s,.!]*/gi, '');

    s = s.replace(/\bma?i?n?\s*ananya\s+(hu|hoon)\b\s*,?\s*(sehat\s*up\s*se)?[\s,.!]*/gi, '');
  }
  s = s.replace(/\b(ji)(\s*,?\s*ji\b)+/gi, 'ji');
  s = s.replace(/(\u091C\u0940)(\s*,?\s*\u091C\u0940)+/g, '\u091C\u0940');
  s = s.replace(/[ \t]{2,}/g, ' ').replace(/\n{3,}/g, '\n\n');

  s = s.replace(/^[\s,.:;!-]+/, '').replace(/[ \t]+([,.!?])/g, '$1').replace(/[\s,;:-]+$/, '').trim();
  return s;
}

const _rawModelText = String(aiText || '');
aiText = sanitizeReply(aiText);

const QR_FILLER_RE = /^(ji|jee|g|ok|okay|okey|k|hmm+|hm|acha|accha|achha|theek|thik|theek hai|thik hai|haan|han|ha|yes|sure|right|ji haan|ji ok|noted|welcome|most welcome)[\s.,!]*$/i;

const QR_NONANSWER_MAX_WORDS = 5;
function qrIsNonAnswer(t) {
  const s = String(t || '').trim();
  if (!s) return true;
  if (QR_FILLER_RE.test(s)) return true;
  if (/\?/.test(s)) return false;

  if (/(rs\.?\s*\d{2,}|\b\d{3,}\s*(rupees|rupaye|rupay)\b|sehatup\.com|https?:\/\/)/i.test(s)) return false;
  return s.split(/\s+/).filter(Boolean).length <= QR_NONANSWER_MAX_WORDS;
}
if (!greetedBefore && QR_FILLER_RE.test(String(aiText || '').trim())) {
  console.log('[Sanitize] first-contact reply was intro-only - blanked so the opener is used | was=' + JSON.stringify(aiText));
  aiText = '';
}

const _modelText = String(aiText || '');

let roleGuard = false;
{

  const _rt = _rawModelText + '\n' + String(aiText || '');

  const _vocativeAnanya =
       /(^|\n)\s*((hi|hello|hey|namaste|dear|good\s*(morning|afternoon|evening))\b[\s,]*)?ananya\s*[,:]/i.test(_rt)
    || /\b(to|dear|thanks|thank\s*you|ok|okay)\s+ananya\b/i.test(_rt);

  const _SYMPTOMS = '(pcod|pcos|thyroid|diabetes|sugar|bp|acne|pigmentation|hair\\s*fall|hairfall|infertility|anxiety|stress|dard|pain|problem|issue)';
  const _patientVoice =
       new RegExp('\\b(i\\s*am|i\\s*m|im)\\s+(suffering|facing|dealing)\\b', 'i').test(_rt)
    || new RegExp('\\bi\\s+(have|had|ve\\s*got|got)\\s+(been\\s+)?' + _SYMPTOMS, 'i').test(_rt)
    || new RegExp('\\bi\\s+(used|tried|took)\\s+(medication|medicine|tablet|tablets|treatment|dawa)', 'i').test(_rt)
    || new RegExp('\\bmujhe\\s+' + _SYMPTOMS + '\\b[^.!?]{0,14}\\b(hai|h)\\b', 'i').test(_rt)
    || new RegExp('\\bmer[ia]\\s+' + _SYMPTOMS + '\\b[^.!?]{0,14}\\b(hai|h)\\b', 'i').test(_rt);

  const _custLines = String(newMsgText || '').split(/\n+/).map(s => s.trim()).filter(Boolean);
  const _replyLines = _rawModelText.split(/\n+/).map(s => s.trim()).filter(Boolean);
  const _lastLine = (_replyLines[_replyLines.length - 1] || '').toLowerCase();
  const _echoesCustomer = _replyLines.length > 1 && !!_lastLine
    && _custLines.some(c => c.toLowerCase() === _lastLine);

  const _norm = (s) => String(s || '').toLowerCase().replace(/[^a-z0-9\s]+/g, ' ').replace(/\s+/g, ' ').trim();
  const _replyTok = _norm(_rawModelText)
    .replace(/\b(good\s*(morning|afternoon|evening)|hello|hi|hey|namaste|ji|sir|mam|madam|ok|okay)\b/g, ' ')
    .split(/\s+/).filter(w => w.length > 1);
  const _custTok = new Set(_norm(newMsgText).split(/\s+/).filter(Boolean));
  const _overlap = _replyTok.length ? _replyTok.filter(w => _custTok.has(w)).length / _replyTok.length : 0;

  const _addsSomething = /(https?:\/\/|sehatup\.com|rs\.?\s*\d{2,}|\?)/i.test(_rawModelText);
  const _parrotsCustomer = _replyTok.length >= 3 && _overlap >= 0.8 && !_addsSomething;

  if (_vocativeAnanya || _patientVoice || _echoesCustomer || _parrotsCustomer) {
    roleGuard = _vocativeAnanya ? 'addressed_ananya'
      : (_patientVoice ? 'patient_voice'
      : (_echoesCustomer ? 'echoed_customer' : 'parroted_customer'));

    aiText = 'Ji bilkul, main aapki help karti hu. Aap thoda detail me bata dijiye - problem kya hai aur kab se hai? '
      + 'Free consultation me doctor aapko sahi plan bata denge, consultation aur diet plan dono free hain.';
    console.log('[Role Guard] BLOCKED ' + roleGuard + ' - model replied as the CUSTOMER'
      + ' | was=' + JSON.stringify(_rawModelText.slice(0, 120)));
  }
}

const _qLower = String(newMsgText || '').toLowerCase();
const _CONDITIONS = '(pcod|pcos|thyroid|hypothyroid|hyperthyroid|diabetes|sugar|bp|blood\\s*pressure|infertility|anemia|anaemia|cancer|hormonal\\s*imbalance)';
const _custRaisedCondition = new RegExp(_CONDITIONS, 'i').test(_qLower);
const _custGaveNumber = /\b(score|skor)\b[^.!?]{0,20}\d/i.test(_qLower);

const gaveScore = !_custGaveNumber && (
     /\b(score|skor)\b[^.!?]{0,24}\b\d{1,3}\b/i.test(aiText)
  || /\b\d{1,3}\s*(\/|out\s*of)\s*(10|100)\b/i.test(aiText)
  || /\b\d{1,3}\s*(me|mein|mai)\s*se\s*\d{1,3}\b/i.test(aiText)
);

function qrAssertsCondition(t) {
  const RE = new RegExp('\\b(aapko|apko|aap\\s*ko|aapka|apka|you\\s*have|you\\s*are\\s*having|u\\s*have)\\b[^.!?]{0,26}\\b' + _CONDITIONS + '\\b', 'i');
  const sentences = String(t || '').split(/(?<=[.!?])\s+|\n+/);
  for (const s of sentences) {
    if (/\?/.test(s)) continue;
    if (/\bkya\b/i.test(s)) continue;
    if (/\b(agar|if|jab|yadi|shayad|ho\s*sakta)\b/i.test(s)) continue;
    if (RE.test(s)) return true;
  }
  return false;
}
const gaveDiagnosis = !_custRaisedCondition && qrAssertsCondition(aiText);

const _rawName = String(name || '').trim();
const _nameIsPlausible = /^[A-Za-z][A-Za-z.'-]{1,20}(\s+[A-Za-z][A-Za-z.'-]{1,20}){0,2}$/.test(_rawName)
  && !/(love|papa|mumm?y|\bmom\b|dad+y?|jaan|baby|king|queen|\bji\b|\bsir\b|madam|boss|bhai|didi|angel|princess|hero|star|customer|unknown)/i.test(_rawName);
let nameGuard = false;

const NAME_MIN_STRIP = 3;
if (_rawName && !_nameIsPlausible && _rawName.length >= NAME_MIN_STRIP) {
  const _esc = _rawName.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
  const _re = new RegExp('\\b' + _esc + '\\b', 'ig');
  const _hits = (aiText.match(_re) || []).length;

  if (_hits > 0 && _hits <= 3) {
    const _stripped = aiText.replace(_re, '')
      .replace(/\s+for\s*$/i, '').replace(/\s*,+\s*,/g, ',')
      .replace(/\s{2,}/g, ' ').replace(/\s+([,.!?])/g, '$1').trim();
    if (_stripped && _stripped.length >= aiText.length * 0.4) {
      aiText = _stripped;
      nameGuard = true;
      console.log('[Name Guard] stripped the WhatsApp profile name | name=' + JSON.stringify(_rawName));
    } else {
      console.log('[Name Guard] SKIPPED - stripping ' + JSON.stringify(_rawName) + ' (' + _hits + ' hits) would have destroyed the reply');
    }
  }
}

const _rs = (n) => 'Rs ' + n;

const _pname = (p) => String(p.title || '').replace(/\s+-\s+/g, ' ').trim();
const _productLine = (p, i) => (i ? i + ') ' : '') + _pname(p) + ' - ' + _rs(p.price) + '\n' + p.url;

const HEALTH_SCORE_URL_2 = 'https://www.sehatup.com/pages/health-score-360';
let reportGuard = false;
let _report = {};
try { _report = ($('Fetch Customer Context').first().json || {}).report || {}; } catch (e) {}

let _automationAnswered = false;
try {
  const _hist = $('Fetch Conversation History').all().map(i => i && i.json).filter(Boolean);
  const _inTimes = _hist.filter(m => m.direction === 'in').map(m => Number(m.msgTime) || 0);
  const _lastIn = _inTimes.length ? Math.max.apply(null, _inTimes) : 0;
  _automationAnswered = _hist.some(m => m.direction === 'out'
    && (m._type === 'BOT_PLACEHOLDER' || m.messageBy === 'AUTOMATION' || !!m.automationBy)
    && (Number(m.msgTime) || 0) >= _lastIn);
  if (_automationAnswered) console.log('[Report Guard] a QuickReply automation already replied to this message');
} catch (e) {}
const _REPORTWORD = '(report|repot|reprot|rport|riport)';
const askedReport = new RegExp('(health|medical)\\s*' + _REPORTWORD + '|\\b' + _REPORTWORD + '\\b|' + _REPORTWORD + '\\s*(chahiye|chaiye|nahi|nhi|nai|kaha|kahan|mila|milega|bhej|send|dijiye|do\\b)', 'i').test(_qLower)
  || /(meri|mera|apni|apna|my)\s+report/i.test(_qLower);

const _reportCtx = /report|assessment|score/i.test(_qLower) || /report|assessment|score/i.test(_lastOutText);
const askedNewReport = _reportCtx && (
     /\bre-?new\b|\brenewal\b/i.test(_qLower)
  || /\b(new|naya|nayi|nayaa|fresh|dubara|dobara|again|update|updated)\b[^.!?]{0,16}\b(report|assessment|test|score)\b/i.test(_qLower)
  || /\b(report|assessment|test|score)\b[^.!?]{0,16}\b(new|naya|nayi|fresh|dubara|dobara|again|update|updated)\b/i.test(_qLower)
  || /phir\s*se[^.!?]{0,16}\b(report|assessment|banwa|karwa)\b/i.test(_qLower)
);
if (_automationAnswered && (askedNewReport || askedReport)) {

  reportGuard = false;
  console.log('[Report Guard] SKIPPED - QuickReply automation already delivered the report | q=' + _qLower.slice(0, 50));
} else if (askedNewReport) {
  reportGuard = 'new_requested';
  aiText = 'Ji, nayi report ke liye assessment dobara complete kar lijiye:\n'
    + HEALTH_SCORE_URL_2
    + '\n\nReport ban jayegi, aur free consultation me doctor aapko usko detail me samjha denge.';
  console.log('[Health Report] new_requested | q=' + _qLower.slice(0, 50));
} else if (askedReport) {
  if (_report.found && _report.url) {
    reportGuard = 'sent';
    aiText = 'Ji, ye rahi aapki health report:\n' + _report.url
      + '\n\nIse download kar lijiye. Free consultation me doctor ise detail me samjha denge, wo bilkul free hai.';
  } else if (_report.found && _report.generating) {
    reportGuard = 'generating';
    aiText = 'Ji, aapki report abhi tayaar ho rahi hai. Ready hote hi hamari team aapko bhej degi. Tab tak koi sawaal ho to bataiye.';
  } else {

    reportGuard = 'no_submission';
    aiText = 'Ji, aapka health assessment abhi complete nahi dikh raha. Yahan se kar lijiye, report ban jayegi:\n'
      + HEALTH_SCORE_URL_2
      + '\n\nUske baad free consultation me doctor aapko sab detail me samjha denge.';
  }
  console.log('[Health Report] ' + reportGuard + ' | url=' + (_report.url ? 'yes' : 'no') + ' | q=' + _qLower.slice(0, 50));
}

const HEALTH_SCORE_URL = 'https://www.sehatup.com/pages/health-score-360';
let scoreGuard = false;
const askedHealthScore = /health\s*score|healthscore|health\s*check|score\s*check|score\s*(chahiye|batao|bata|check)/i.test(_qLower);
if (!reportGuard && askedHealthScore) {
  scoreGuard = true;
  aiText = 'Ji, apna free health score aap yahan check kar sakte hain:\n' + HEALTH_SCORE_URL
    + '\n\nYe bilkul free hai. Score aane ke baad free consultation me doctor aapko detail me samjha denge.';
  console.log('[Health Score] sent the official link | q=' + _qLower.slice(0, 60));
}

let identityGuard = false;
if (!reportGuard && !scoreGuard && _askedIdentity) {
  identityGuard = true;
  aiText = 'Mai Ananya hu ji, SehatUP ki health advisor. SehatUP India ki digital clinic hai - '
    + 'Ayurveda, Homeopathy aur modern medicine ke doctors ek saath, aur consultation bilkul free hai. '
    + 'Aap apni health ki koi bhi problem bata dijiye, main help kar deti hu.';
  console.log('[Identity] answered a who-are-you question | q=' + _qLower.slice(0, 60));
}

let companyGuard = false;

const _orderish = /\b(order|parcel|shipment|courier|delivery|awb|track|tracking|refund|return)\b/i.test(_qLower);
const _askedWhereFrom = !_orderish && (
     /\b(kaha|kahan|kahaa)\s*se\b[^.!?]{0,20}\b(aap|ho|hain|bol|baat|call|ka?r)/i.test(_qLower)
  || /\b(aap|aapka|apka|tum|tumhara|ye|yah|sehatup|company|office)\b[^.!?]{0,20}\b(kaha|kahan|kahaa)\b/i.test(_qLower)
  || /\bwhere\b[^.!?]{0,20}\b(are\s*you|from|based|located)\b/i.test(_qLower)
  || /\b(company|office|address|pata)\b[^.!?]{0,20}\b(kaha|kahan|kya|kis|batao|bataiye)\b/i.test(_qLower)
);

const _askedRecording = /\b(recording|record\s*kar|voice\s*(note|msg|message)?|audio|awaz|awaaz)\b/i.test(_qLower);

let claimGuard = false;
if (!reportGuard && !scoreGuard && !identityGuard && (gaveScore || gaveDiagnosis)) {
  claimGuard = gaveScore ? 'invented_score' : 'invented_diagnosis';
  aiText = 'Ji, main khud se koi score ya health condition nahi bata sakti - ye doctor properly check karke hi batate hain. Aap apna naam aur kaunsa time aapke liye theek rahega bata dijiye, main free consultation set kara deti hu.';
  console.log('[Claim Guard] BLOCKED ' + claimGuard + ' | q=' + _qLower.slice(0, 60));
}

const DOSE_HANDOFF = 'Dose doctor hi batate hain ji. Aap apna naam aur kaunsa time aapke liye theek rahega bata dijiye, main free consultation set kara deti hu - doctor sab detail me samjha denge.';
const _q = String(newMsgText || '').toLowerCase();

const _DOSE_VERB = /(len[aeiu]|lene|khan[aeiu]|khaye|khaun|pee?n[aei]|piye|piyu|\btake\b|\bconsume\b|istemal|use\s*kar)/;
const askedDose =
     /\bdos(e|es|age|ages)\b/.test(_q)
  || /khura+k|\bmatra\b|\bmaatra\b/.test(_q)
  || /(kitni|kitne|kitna|kitnii)\s+(\S+\s+){0,2}(goli|goliy|tablet|tablets|capsule|cap\b|churan|churna|chammach|spoon|boond|drop|drops|scoop|sachet|\bml\b|\bmg\b)/.test(_q)
  || (/(kitni|kitne)\s+(\S+\s+){0,2}(din|days|baar|dafa|hafte|weeks|mahine|months)/.test(_q) && _DOSE_VERB.test(_q))

  || /(kitna|kitni|kitne)\s+(\S+\s+){0,2}(len[aeiu]|lene|leni|khan[aeiu]|khani|khaye|khaun|pee?n[aei]|piye|piyu|istemal|use)/.test(_q)

  || /(kaise|kese|kaisay|kab|kis\s*time|kis\s*samay|kis\s*tarah|kitne\s*din|kitni\s*der|khali\s*pet)\s*(\S+\s+){0,3}(len[aei]|lene|leni|khan[aei]|khani|khaye|khaun|pee?n[aei]|piye|piyu|use|istemal|isteml|upyog|upayog|upyaog|lagan[aei]|lagani)/.test(_q)
  || /\bhow\s+(many|much|often|long)\b/.test(_q)
  || /\b(when|how)\s+(should\s+i\s+|do\s+i\s+|to\s+)?(take|consume|use)\b/.test(_q)
  || /(empty\s*stomach|before\s+(food|meal)|after\s+(food|meal))/.test(_q);

const _a = String(aiText || '').toLowerCase();

const _NOT_A_DOSE = '(?!\\s*(bhi\\s+)?(band|bnd|mat|nahi|nhi|na\\b|kam|chhod|chod|skip|miss|rok))';
const gaveDose =
     new RegExp('\\b\\d+\\s*(-|to|se|se\\s*\\d+)?\\s*(goli|goliya|goliyan|tablet|tablets|capsule|capsules|chammach|spoon|boond|drop|drops|scoop|sachet)\\b' + _NOT_A_DOSE).test(_a)
  || new RegExp('\\b(ek|do|teen|char|one|two|three|four)\\s+(goli|goliya|goliyan|tablet|tablets|capsule|capsules|chammach|spoon|boond|drop|drops|scoop|sachet)\\b' + _NOT_A_DOSE).test(_a)
  || /\b(once|twice|thrice)\s+(a\s+)?(day|daily)\b/.test(_a)
  || /\b(din|roz|rozana)\s*(me|mein)?\s*(ek|do|teen|\d+)\s*baar\b/.test(_a);

let consultModeGuard = false;
const _answered = () => !!(reportGuard || scoreGuard || identityGuard || claimGuard || companyGuard || consultModeGuard);

const doseGuard = !_answered() && (askedDose || gaveDose);
if (doseGuard) {
  console.log('[Dose Guard] replaced reply | askedDose=' + askedDose + ' gaveDose=' + gaveDose + ' | q=' + _q.slice(0, 60));
  aiText = DOSE_HANDOFF;
}

const WORK_START_MIN = 9 * 60 + 30;
const WORK_END_MIN = 18 * 60 + 30;
const _hhmm = (mins) => {
  const h = Math.floor(mins / 60), m = mins % 60;
  return (((h + 11) % 12) + 1) + (m ? ':' + String(m).padStart(2, '0') : '') + (h < 12 ? ' AM' : ' PM');
};

function qrParseSlot(text) {
  const s = String(text || '').toLowerCase();
  const re = /(\d{1,2})(?:[:.](\d{2}))?\s*(a\.?m\.?|p\.?m\.?|baje|bje|bajey|bajay|baze|bajke|o'?\s?clock)/g;
  let m, best = null;
  while ((m = re.exec(s)) !== null) {
    let h = parseInt(m[1], 10);
    const min = m[2] ? parseInt(m[2], 10) : 0;
    if (h < 1 || h > 24 || min > 59) continue;
    const marker = m[3] || '';
    const around = s.slice(Math.max(0, m.index - 26), m.index + m[0].length + 26);
    let pm = null;
    if (/^p/.test(marker)) pm = true;
    else if (/^a/.test(marker)) pm = false;
    else if (/(raat|raat\s*ko|\brat\b|night|shaam|\bsham\b|evening|dopahar|dopeher|afternoon)/.test(around)) pm = true;
    else if (/(subah|savere|sawere|\bsubh\b|morning)/.test(around)) pm = false;

    else pm = (h >= 1 && h <= 6);
    if (pm && h < 12) h += 12;
    if (!pm && h === 12) h = 0;
    if (h === 24) h = 0;
    best = h * 60 + min;
  }
  return best;
}

let slotGuard = false;
const _slotMin = qrParseSlot(newMsgText);
if (!_answered() && !doseGuard && _slotMin !== null && (_slotMin < WORK_START_MIN || _slotMin > WORK_END_MIN)) {
  slotGuard = true;
  aiText = _hhmm(_slotMin) + ' par consultation nahi ho paayegi ji, hamare doctor '
    + _hhmm(WORK_START_MIN) + ' se ' + _hhmm(WORK_END_MIN)
    + ' tak available rehte hain. Is beech ka koi time bata dijiye, main slot fix kara deti hu.';
  console.log('[Slot Guard] requested ' + _hhmm(_slotMin) + ' is outside '
    + _hhmm(WORK_START_MIN) + '-' + _hhmm(WORK_END_MIN) + ' | q=' + String(newMsgText || '').slice(0, 60));
}

let bookingGuard = false;

const _weAskedForTime = /kaunsa time|naam aur|time aapke liye theek|time bata dijiye|kis time|konsa time/i.test(_lastOutText);

function qrParseBareTime(text) {
  const m = String(text || '').trim().match(/^\s*(\d{1,2})(?:[:.]?(\d{2}))?\s*$/);
  if (!m) return null;
  let h = parseInt(m[1], 10);
  const min = m[2] ? parseInt(m[2], 10) : 0;
  if (h < 1 || h > 24 || min > 59) return null;
  if (h >= 1 && h <= 6) h += 12;
  if (h === 24) h = 0;
  return h * 60 + min;
}

const _replyWords = String(newMsgText || '').trim().split(/\s+/).filter(Boolean);
const _NOT_A_NAME = /^(ji|jee|ok|okay|haan|han|ha|yes|no|nahi|nhi|hmm|acha|accha|theek|thik|hi|hello|hey|namaste|kya|kyu|kyun|kaisa|kaise|kaisi|kab|kahan|kaun|kaunsa|konsa|mera|meri|aapka|apka|aap|main|mai|hai|h|price|order|product|dawa|medicine|consultation|time|naam)$/i;
const _isNameWord = (x) => /^[A-Za-z][A-Za-z.'-]{1,20}$/.test(x || '') && !_NOT_A_NAME.test(x || '');
let _givenName = '';

const _nameStated = String(newMsgText || '').match(/(?:mera\s+naam|my\s+name\s+is|naam\s+hai|i\s*am)\s+([A-Za-z][A-Za-z.'-]{1,20})/i);
if (_nameStated && _isNameWord(_nameStated[1])) _givenName = _nameStated[1];
else if (_replyWords.length >= 1 && _replyWords.length <= 2 && _replyWords.every(_isNameWord)) _givenName = _replyWords.join(' ');
const _looksLikeName = !!_givenName;
const _haveName = !!(buildData.displayName || '').trim();

if (!_answered() && !doseGuard && !slotGuard && _weAskedForTime) {
  const _t = (_slotMin !== null) ? _slotMin : qrParseBareTime(newMsgText);
  if (_t !== null && (_t < WORK_START_MIN || _t > WORK_END_MIN)) {
    bookingGuard = 'out_of_hours';
    aiText = _hhmm(_t) + ' par consultation nahi ho paayegi ji, hamare doctor '
      + _hhmm(WORK_START_MIN) + ' se ' + _hhmm(WORK_END_MIN)
      + ' tak available rehte hain. Is beech ka koi time bata dijiye.';
  } else if (_t !== null) {
    bookingGuard = 'time_taken';
    aiText = 'Ji, ' + _hhmm(_t) + ' note kar liya. '
      + (_haveName
        ? 'Hamari team us time par aapko call karegi.'
        : 'Aapka naam bhi bata dijiye, main consultation confirm kar deti hu.');
  } else if (_looksLikeName && !_haveName) {
    bookingGuard = 'name_taken';
    aiText = 'Shukriya ' + _givenName + ' ji. Ab bas ye bata dijiye ki kaunsa time aapke liye theek rahega ('
      + _hhmm(WORK_START_MIN) + ' se ' + _hhmm(WORK_END_MIN) + ' ke beech)?';
  }
  if (bookingGuard) console.log('[Booking Guard] ' + bookingGuard + ' | reply=' + JSON.stringify(String(newMsgText || '').slice(0, 30)));
}

let bookingClaimGuard = false;
{

  const _bookingCtx = String(aiText) + ' ' + String(_lastOutText) + ' ' + String(newMsgText);
  const _isBookingSubject = /\b(consultation|appointment|slot|booking)\b/i.test(_bookingCtx);
  const _notBookingSubject = _orderish
    || /\b(refund|return|wapas|wapsi|cancel|order|parcel|courier|tracking)\b/i.test(_bookingCtx)
    || /\b(hospital|emergency|ambulance|dangerous|oncologist|gynaecologist|gynecologist)\b/i.test(String(aiText));

  const _claimsBooked = _isBookingSubject && !_notBookingSubject && (
       /\b(request|details|detail|jaankari|naam)\b[^.!?]{0,26}\b(bhej\s*di|bhej\s*diya|bhej\s*dia|share\s*kar\s*di|share\s*kar\s*diya|de\s*di|de\s*diya)\b/i.test(String(aiText))
    || /\b(team|doctor)\b[^.!?]{0,26}\b(connect|call|contact|sampark)\b[^.!?]{0,20}\b(karegi|karenge|kar\s*legi|kar\s*lenge)\b/i.test(String(aiText))
    || /\b(consultation|appointment|slot|booking)\b[^.!?]{0,24}\b(fix\s*ho|book\s*ho|set\s*ho|ho\s*gay[ai]|kar\s*di\b|kar\s*diya\b|fix\s*kar\s*di\b)\b/i.test(String(aiText))

    || /\b(consultation|appointment|slot|booking)\b[^.!?]{0,30}\b(book|set|fix)\b[^.!?]{0,12}\b(kar|kara|karwa)\b[^.!?]{0,10}\b(deti|dungi|denge|diya|di|liya)\b/i.test(String(aiText))
  );

  const _alsoAsks = /(kaunsa time|kis time|konsa time|time bata|naam bata|naam aur|ke beech)/i.test(String(aiText));

  if (_claimsBooked && !_alsoAsks && !bookingGuard) {

    let _gaveTimeEver = false, _gaveNameEver = _haveName;
    try {
      const _ins = $('Fetch Conversation History').all().map(i => i && i.json)
        .filter(m => m && m.direction === 'in' && m.text);
      for (const m of _ins) {
        const t = String(m.text || '');
        if (qrParseSlot(t) !== null || qrParseBareTime(t) !== null) _gaveTimeEver = true;
        if (/(?:mera\s+naam|my\s+name\s+is|naam\s+hai)\s+[A-Za-z]/i.test(t)) _gaveNameEver = true;
      }
    } catch (e) {}
    if (!_gaveTimeEver) {
      bookingClaimGuard = _gaveNameEver ? 'no_time' : 'no_name_no_time';
      aiText = 'Ji bilkul, main consultation set kara deti hu. '
        + (_gaveNameEver ? '' : 'Bas apna naam bata dijiye aur ')
        + 'kaunsa time aapke liye theek rahega (' + _hhmm(WORK_START_MIN) + ' se '
        + _hhmm(WORK_END_MIN) + ' ke beech)? Doctor us time par aapko call karenge.';
      console.log('[Booking Claim Guard] ' + bookingClaimGuard
        + ' - reply claimed a booking with no time on record | was=' + JSON.stringify(String(aiText).slice(0, 80)));
    }
  }
}

let priceGuard = false;
let _matches = [];
try { _matches = ($('Fetch Product Matches').first().json || {}).matches || []; } catch (e) {}

const _pq = String(newMsgText || '').toLowerCase();
const askedPrice =
     /\b(price|rate|cost|mrp)\b/.test(_pq)
  || /\b(keemat|kimat|kimmat|daam|dam)\b/.test(_pq)
  || /kitne?\s*(ka|ki|ke)\b/.test(_pq)
  || /\bkitna\s*(hai|h|hoga|padega)\b/.test(_pq)
  || /\bhow\s+much\b/.test(_pq);

const replyHasPrice = /(rs\.?\s*\d{2,}|\u20B9\s*\d{2,}|\b\d{3,}\s*(rupees|rupaye|rupay|rs)\b)/i.test(String(aiText));

const askedLink = /\b(link|url)\b/.test(_pq) || /\b(bhej\s*d(o|ijiye|ena)|bhejo|send\s*kar|share\s*kar)\b/.test(_pq);
const replyHasLink = /sehatup\.com\/products\//i.test(String(aiText));

const _PRODUCT_NOUN = '(product|products|dawa|dawai|medicine|item|option|options|kit|combo|pack'
  + '|drops?|boond|goli|goliyan|tablet|tablets|capsule|capsules|churna|churan|powder'
  + '|tel|oil|syrup|tea|bati|vati|resin|sachet|cream|gel)';
const askedProduct =
     /\b(chahiye|chaiye|chahie|mangwana|mangana|khareed|buy|order\s*karna|lena\s*(hai|h)\b)/i.test(_pq)
  || new RegExp('\\b(konsa|konse|kaunsa|kaunse|kaun\\s*sa|which|what)\\b[^.!?]{0,24}\\b' + _PRODUCT_NOUN + '\\b', 'i').test(_pq)
  || new RegExp('\\b' + _PRODUCT_NOUN + '\\b[^.!?]{0,24}\\b(batao|bata|bta|dikhao|dikha|suggest|recommend|hai|\\bh\\b|available|milega|milta)', 'i').test(_pq)

  || new RegExp('\\b(kya|kaun|konsa|kaunsa|kaunse|kis)\\b\\s*(\\S+\\s+){0,2}(chij|cheez|chiz|chize|cheez)\\b', 'i').test(_pq)
  || new RegExp('\\b(kya|kaun|konsa|kaunsa|kis)\\b\\s*(\\S+\\s+){0,2}' + _PRODUCT_NOUN + '\\b', 'i').test(_pq)
  || /\b(recommend|suggest)\b/i.test(_pq)
  || /\bke\s*liye\b[^.!?]{0,24}\b(kya|konsa|konse|kaunsa|product)\b/i.test(_pq);

const _replyNamesAMatch = _matches.some(p => {
  const key = String(p.title || '').split(/[^A-Za-z0-9]+/).filter(w => w.length > 3)[0];
  return key && new RegExp(key.replace(/[.*+?^${}()|[\]\\]/g, '\\$&'), 'i').test(String(aiText));
});

const needsPrice = askedPrice && !replyHasPrice;

const ignoredTheCatalog = askedProduct && !_replyNamesAMatch && !replyHasPrice && !replyHasLink;
const needsLink = askedLink && !replyHasLink;

if (!_answered() && !doseGuard && !slotGuard && !bookingGuard && (needsPrice || needsLink || ignoredTheCatalog) && _matches.length) {
  const sellable = _matches.filter(p => !p.isRx && p.inStock);
  const rx = _matches.filter(p => p.isRx);
  const oos = _matches.filter(p => !p.isRx && !p.inStock);
  const CONSULT = 'Chahein to free consultation me doctor aapke liye best option confirm kar denge, wo free hai.';

  const CONSULT_HANDLES = /(her-menses|harmen|hormoniherb|tea-for-period-cramps|aloezy|thyrostatin|diaboglob|vitamin-d3k2|zencal)/i;
  const CONSULT_TOPIC_RE = /\b(pcod|pcos|poly\s*cystic|period|periods|menses|menstrual|cramp|cramps|hormonal|hormone|thyroid|thyro|sugar|diabetes|diabetic|bp|blood\s*pressure|vitamin\s*d|d3|calcium|haddi|bone|pregnan|breastfeed|heart|kidney|liver)\b/i;
  const _consultTopic = CONSULT_TOPIC_RE.test(_pq)
    || _matches.some(p => CONSULT_HANDLES.test(String(p.url || '') + ' ' + String(p.title || '')));

  const CAUTION = 'Koi health problem ho ya koi dawai chal rahi ho to bata dijiye, main doctor se check kara deti hu - warna aap order kar sakte hain.';
  const CLOSE = _consultTopic ? CONSULT : CAUTION;

  const kit = sellable.find(p => p.isKit);
  const parts = sellable.filter(p => !p.isKit);
  const namedParts = parts.filter(p => (p.named || 0) > 0);
  if (namedParts.length >= 2) {

    const [a, b] = namedParts;
    const sum = a.price + b.price;
    aiText = 'Ji, dono ke rate ye hain:\n\n' + _productLine(a, 1) + '\n\n' + _productLine(b, 2);

    if (kit && kit.price < sum) {
      aiText += '\n\nDono ka combo ' + _pname(kit) + ' me ' + _rs(kit.price)
        + ' me mil jata hai, alag-alag lene par ' + _rs(sum) + ' padta hai:\n' + kit.url;
    }
    aiText += '\n\n' + CLOSE;
    priceGuard = (kit && kit.price < sum) ? 'both_with_combo' : 'both';
  } else if (parts.length === 1) {
    const p = parts[0];
    aiText = _pname(p) + ' ka price ' + _rs(p.price) + ' hai ji.\n' + p.url + '\n\n' + CLOSE;
    priceGuard = ignoredTheCatalog && !needsPrice && !needsLink ? 'recommended' : 'one';
  } else if (parts.length > 1) {

    const [a, b] = parts;
    aiText = 'Ji, do options hain:\n\n'
      + _productLine(a, 1) + '\n\n'
      + _productLine(b, 2) + '\n\n'
      + 'Aapko kaunsa chahiye - 1 ya 2?';
    priceGuard = ignoredTheCatalog && !needsPrice && !needsLink ? 'recommended_2' : 'ambiguous';
  } else if (kit) {

    aiText = _pname(kit) + ' ka price ' + _rs(kit.price) + ' hai ji.\n' + kit.url + '\n\n' + CLOSE;
    priceGuard = 'kit';
  } else if (rx.length) {
    aiText = rx[0].title + ' me allopathic medicine hoti hai ji, iske liye doctor ki salah zaroori hai - iska price share nahi kar sakti. 10-15 min ki free consultation kara deti hu.';
    priceGuard = 'rx';
  } else if (oos.length) {
    aiText = oos[0].title + ' abhi available nahi hai ji. ' + (_consultTopic ? 'Free consultation me doctor aapke liye sahi alternative bata denge, consultation aur diet plan free hai.' : 'Hamari team aapko iska sahi alternative bata degi.');
    priceGuard = 'out_of_stock';
  }
  if (priceGuard) {
    console.log('[Price Guard] ' + priceGuard + ' | q=' + _pq.slice(0, 50)
      + ' | matches=' + _matches.map(p => p.title + '=' + p.price + (p.isRx ? '/Rx' : '') + (p.inStock ? '' : '/OOS')).join(', '));
  }
}

if (!_answered() && !doseGuard && !slotGuard && !bookingGuard && !priceGuard
    && (_askedWhereFrom || _askedRecording)) {
  companyGuard = _askedWhereFrom ? (_askedRecording ? 'where_from_voice' : 'where_from') : 'voice_note';
  aiText = (_askedRecording ? 'Main voice note nahi bhej sakti ji, isliye likh kar bata rahi hu. ' : '')
    + (_askedWhereFrom

      ? 'SehatUP India ki digital clinic hai - Ayurveda, Homeopathy aur modern medicine ke doctors ek saath. '
        + 'Sab kuch online aur phone par hota hai, isliye aap kisi bhi shehar me ho hum help kar dete hain. '
        + 'Hum AYUSH-approved aur GMP-certified hain, aur call hamare verified business number se hi aati hai. '
        + 'Aur kuch jaanna ho to bata dijiye.'
      : 'Aap jo jaanna chahte hain wo likh kar puch lijiye, main detail me bata deti hu.');
  console.log('[Company Guard] ' + companyGuard + ' | q=' + _qLower.slice(0, 60));
}

const _payish = /\b(pay|payment|paid|paisa|paise|rupee|rupay|cod|prepaid|upi|card|netbanking|emi)\b/i.test(_qLower);
const _consultModeRe = new RegExp(
     '\\b(online|offline|on\\s*line|off\\s*line)\\b'
  + '|\\b(clinic|hospital|centre|center|branch|showroom)\\b'
  + '|\\b(video\\s*call|phone\\s*(pe|par|call)|call\\s*(pe|par))\\b'
  + '|\\b(aana|ana|aanaa|jana|jaana|visit|milna|milne|aake|aakar)\\b[^.!?]{0,18}\\b(padega|padegi|padta|padti|hoga|hogi|hai|h|kya)\\b'
  + '|\\bconsultation\\b[^.!?]{0,18}\\b(kaise|kese|kahan|kaha|kidhar)\\b'
  + '|\\b(kaise|kese|kahan|kaha|kidhar)\\b[^.!?]{0,18}\\b(consultation|consult|checkup|check\\s*up)\\b', 'i');
if (!_answered() && !doseGuard && !slotGuard && !bookingGuard && !priceGuard
    && !_orderish && !_payish && _consultModeRe.test(_qLower)) {
  consultModeGuard = true;
  aiText = 'Ji, consultation puri tarah online hoti hai - doctor aapko call karke baat karte hain, '
    + 'aapko kahin aana nahi padta. 10-15 minute lagte hain aur consultation bilkul free hai. '
    + 'Aap apna naam aur kaunsa time aapke liye theek rahega bata dijiye ('
    + _hhmm(WORK_START_MIN) + ' se ' + _hhmm(WORK_END_MIN) + ' ke beech)?';
  console.log('[Consult Mode Guard] answered online-vs-offline | q=' + _qLower.slice(0, 60));
}

let promiseGuard = false;
const _CALLWORD = '(call|phone|ring|baat|connect|contact|sampark)';
const _MINS = '(?<!\\d\\s?-\\s?)\\b\\d{1,2}\\s*(minute|minutes|min|mins|minit|minut)\\b';
const _promiseRe = new RegExp(
  _MINS + '[^.!?]{0,30}\\b' + _CALLWORD + '\\b'
  + '|\\b' + _CALLWORD + '\\b[^.!?]{0,30}' + _MINS
  + '|\\b(abhi|turant|immediately|right\\s*now|just\\s*now)\\b[^.!?]{0,26}\\b' + _CALLWORD + '\\b'
  + '|\\b' + _CALLWORD + '\\b[^.!?]{0,26}\\b(abhi|turant|immediately|right\\s*now|just\\s*now)\\b', 'i');

if (!_answered() && !doseGuard && !slotGuard && !bookingGuard && !priceGuard && _promiseRe.test(String(aiText))) {
  promiseGuard = true;
  aiText = 'Ji, main aapki free consultation set kara deti hu. Aapka naam aur kaunsa time aapke liye theek rahega bata dijiye ('
    + _hhmm(WORK_START_MIN) + ' se ' + _hhmm(WORK_END_MIN) + ' ke beech), doctor us time par call karenge.';
  console.log('[Promise Guard] replaced an invented callback promise | q=' + String(newMsgText || '').slice(0, 50));
}

let pitchGuard = false;
const _CATALOG_NAME_RE = new RegExp('\\b(vaji\\s*bati|kern\\s*drops?|shilajit|ashwagandha|her\\s*menses|harmen'
  + '|hormoniherb|aloezy|leanroutine|slimtox|garcinia|diaboglob|thyrostatin|zencal|shaktisurge'
  + '|boombatti|control\\s*tantra|fourplay|hard\\s*yatra|max\\s*drive|rocket\\s*ras|lovelinga'
  + '|thrill\\s*drill|thrustrx|endless|tadalafil|dapoxetine|orlistat)\\b', 'i');

const _pitchThing = /\b(ye|yeh|yah|is|isme|ismein)\b[^.!?]{0,24}\b(ayurvedic|herbal|homeopathic|allopathic)?\s*(medicine|dawa|dawai|tablet|goli|goliyan|drops?|churna|powder|kit|capsule|syrup|tea|product)\b/i.test(String(aiText));
const _pitchClaim = /\b(behtar|behter|bahter|bahtar|improve|badhata|badhati|badhane|theek\s*karta|thik\s*karta|fayda|faida|asar\s*karta|help\s*karta|kaam\s*karta|sex\s*life|stamina|performance)\b/i.test(String(aiText));
if (!_answered() && !doseGuard && !slotGuard && !bookingGuard && !priceGuard && !promiseGuard
    && !_matches.length && !askedProduct && !askedPrice && !askedLink
    && (_CATALOG_NAME_RE.test(String(aiText)) || (_pitchThing && _pitchClaim))) {
  pitchGuard = _CATALOG_NAME_RE.test(String(aiText)) ? 'named_product' : 'described_product';
  aiText = 'Ji bataiye, aapko kya problem ho rahi hai aur kab se? Aapki baat sun kar main sahi guidance de deti hu - '
    + 'free consultation me doctor aapke liye best plan batayenge, consultation aur diet plan dono free hain.';
  console.log('[Pitch Guard] BLOCKED ' + pitchGuard + ' - no catalog match and no product asked'
    + ' | q=' + String(newMsgText || '').slice(0, 50));
}

let substanceGuard = false;
const _custWords = String(newMsgText || '').trim().split(/\s+/).filter(Boolean).length;
const _replyIsFiller = qrIsNonAnswer(aiText);
const _custSaidSomething = _custWords >= 12 || String(newMsgText || '').trim().length >= 70;
const _weAskedQuestion = /\?/.test(_lastOutText)
  || /\b(bata\s*d(o|ijiye|ena)|bataiye|batao|bta\s*d(o|ijiye)|kitni|kitna|kitne|kab\s*se|kaunsa|konsa|kaun\s*sa|kya\s*problem|naam)\b/i.test(_lastOutText);

const _healthOrProductQ = _matches.length > 0
  || /\b(dawa|dawai|medicine|product|tablet|goli|capsule|drops?|churna|powder|kit|combo|price|dose|khurak|matra|problem|pareshani|dard|symptom|ilaj|treatment|fayda|faida|asar|side\s*effect|istemal|upyog|upayog|health|body|weight|skin|hair)\b/i.test(String(newMsgText || ''));
const _custAskedQuestion = /\?/.test(String(newMsgText || ''))
  || /\b(kya|kyu|kyun|kyo|kyon|kaise|kese|kaisa|kaisi|kab|kahan|kaha|kaun|kaunsa|konsa|kis|kisliye|kitna|kitni|kitne|batao|bataiye|bta|btao|btaiye|samjhao|samjhaiye|explain|tell)\b/i.test(String(newMsgText || ''));
if (!_answered() && !doseGuard && !slotGuard && !bookingGuard && !priceGuard && !promiseGuard && !pitchGuard
    && _replyIsFiller && (_custSaidSomething || _weAskedQuestion || _custAskedQuestion)) {

  substanceGuard = _custSaidSomething ? 'long_message'
    : (_custAskedQuestion ? 'unanswered_question'
    : (_weAskedQuestion ? 'answered_our_question' : 'unanswered_question'));

  aiText = substanceGuard === 'long_message'
    ? 'Ji, aapne jo bataya wo maine samajh liya - aap pareshaan mat hoiye, isko manage kiya ja sakta hai. '
      + 'Free consultation me doctor aapki puri history dekh kar sahi plan batayenge, consultation aur diet plan dono free hain. '
      + 'Aap apna naam aur kaunsa time aapke liye theek rahega bata dijiye?'
    : substanceGuard === 'answered_our_question'

    ? 'Ji, note kar liya. Aap apna naam aur kaunsa time aapke liye theek rahega bata dijiye ('
      + _hhmm(WORK_START_MIN) + ' se ' + _hhmm(WORK_END_MIN) + ' ke beech), main free consultation set kara deti hu - doctor aapko sahi plan batayenge.'

    : _healthOrProductQ
    ? 'Ji, iske baare me doctor aapko theek se samjha denge - free consultation me sab detail mil jayegi, wo bilkul free hai. '
      + 'Aap apna naam aur kaunsa time aapke liye theek rahega bata dijiye ('
      + _hhmm(WORK_START_MIN) + ' se ' + _hhmm(WORK_END_MIN) + ' ke beech)?'
    : 'Ji, ye main hamari team se confirm karke aapko bata deti hu. Tab tak aapko health ya product se juda koi sawaal ho to bata dijiye, main abhi help kar deti hu.';
  console.log('[Substance Guard] ' + substanceGuard + ' | custWords=' + _custWords
    + ' | reply=' + JSON.stringify(String(aiText).slice(0, 30))
    + ' | lastOut=' + JSON.stringify(String(_lastOutText).slice(0, 40)));
}

let repeatGuard = false;
const _normReply = (s) => String(s || '').toLowerCase().replace(/[^a-z0-9 ]+/g, ' ').replace(/\s+/g, ' ').trim();
if (!_answered() && !doseGuard && !slotGuard && !bookingGuard && !priceGuard && !promiseGuard && !substanceGuard && !pitchGuard) {
  const _now = _normReply(aiText), _prev = _normReply(_lastOutText);
  if (_now && _prev && (_now === _prev || (_now.length >= 12 && (_prev.includes(_now) || _now.includes(_prev))))) {
    repeatGuard = true;
    aiText = 'Ji, lagta hai main aapko theek se samjha nahi paayi. Free consultation me doctor aapke saare sawaal ka jawaab de denge, wo bilkul free hai. '
      + 'Aap apna naam aur kaunsa time aapke liye theek rahega bata dijiye ('
      + _hhmm(WORK_START_MIN) + ' se ' + _hhmm(WORK_END_MIN) + ' ke beech)?';
    console.log('[Repeat Guard] reply repeated the previous one | was=' + JSON.stringify(String(_lastOutText).slice(0, 50)));
  }
}

let inventGuard = false;
{
  const _t = String(aiText || '');
  const _hasLink = /https?:\/\/|sehatup\.com/i.test(_t);

  const _claimedSend = !_hasLink
    && /\b(maine|mai\s*ne|main\s*ne)\b[^.!?]{0,34}\b(link|url|report|pdf|catalog|catalogue|price\s*list|details|jaankari)\b[^.!?]{0,20}\b(bhej|send|share|forward)\b/i.test(_t);

  const _claimedConfirm = /\b(team|doctor|manager|senior)\b[^.!?]{0,24}\b(se)\b[^.!?]{0,20}\b(confirm|pooch|puch|baat)\b[^.!?]{0,16}\b(kar\s*li\b|kar\s*liya\b|kar\s*diya\b|kar\s*di\b|liya\b)/i.test(_t)
    || /\b(confirm|approve|approval)\b[^.!?]{0,20}\b(kar\s*li\b|kar\s*liya\b|ho\s*gaya\b|mil\s*gaya\b)/i.test(_t);

  const _offeredDiscount = /\b(discount|chhut|chhoot|chut\b|offer|kam\s*kar|kam\s*karwa|kam\s*me|sasta|rebate)\b/i.test(_t)
    && /\b(de\s*d|dedun|de\s*dun|kar\s*d|karwa|dilwa|mil\s*ja|ho\s*ja|check\s*kar|confirm)\b/i.test(_t);

  const _claimedViewed = /\b(maine|mai\s*ne|main\s*ne)\b[^.!?]{0,24}\b(dekh|dekha|dekhi|dekhli)\w*\b/i.test(_t)
    || /\b(dekh\s*liya|dekh\s*li|dekh\s*lia|check\s*kar\s*liya)\b/i.test(_t);

  if (_claimedSend || _claimedConfirm || _offeredDiscount || _claimedViewed) {
    inventGuard = _offeredDiscount ? 'offered_discount'
      : (_claimedViewed ? 'claimed_viewed'
      : (_claimedConfirm ? 'claimed_confirmation' : 'claimed_sent'));
    aiText = _offeredDiscount
      ? 'Ji, price wahi rehta hai - isme main khud se koi badlaav nahi kar sakti. Koi offer chal raha ho to hamari team aapko bata degi. Aap chahein to main aapki free consultation set kara deti hu, wo bilkul free hai.'
      : (_claimedViewed
      ? 'Ji, mujhe yahan photo ya video nahi dikhte, isliye main dekh nahi paayi. Aap likh kar bata dijiye - problem kya hai aur kab se hai? Doctor ko aap call par sab detail me bata sakte hain, wo puri baat samajh kar hi salah denge.'
      : 'Ji, main ye khud se confirm nahi kar sakti. Main aapki request team ko bhej deti hu, wo aapse jaldi connect karenge. Tab tak koi aur sawaal ho to bata dijiye.');
    console.log('[Invent Guard] BLOCKED ' + inventGuard + ' | was=' + JSON.stringify(_t.slice(0, 100)));
  }
}

let aiIdentityGuard = false;
if (/\b(a\.?\s?i\.?|artificial\s*intelligence|bot|chat\s*bot|chatbot|assistant|language\s*model|virtual\s*assistant|machine|program|software)\b/i.test(String(aiText))
    && /\b(hu|hoon|hun|am|is|ek)\b/i.test(String(aiText))) {
  aiIdentityGuard = true;
  aiText = 'Mai Ananya hu ji, SehatUP ki health advisor. Aap apni health se judi koi bhi baat bata dijiye, main help kar deti hu.';
  console.log('[AI Identity Guard] model claimed to be an AI/bot');
}

let pregnancyGuard = false;
{
  var _asksPregnancy = /\b(pregnan\w*|pregnency|garbh\w*|breast\s*feed\w*|breastfeed\w*|feeding\s*kara|doodh\s*pila|conceiv\w*)\b/i.test(String(aiText))
    && /\?|\bnahi\s*hai\b|\bto\s*nahi\b|\bkya\b/i.test(String(aiText));
  if (_asksPregnancy) {
    var _said = String(newMsgText || '');
    try {
      var _ins = $('Fetch Conversation History').all().map(i => i && i.json)
        .filter(m => m && m.direction === 'in' && m.text).map(m => String(m.text));
      _said = _ins.join(' ') + ' ' + _said;
    } catch (e) {}
    var _maleConcern = /\b(timing|time\s*kam|jaldi\s*nikal|nikal\s*jata|discharge|premature|erection|erect\w*|ling|penis|land\b|size\s*chota|chota\s*size|mota\s*karna|veerya|virya|shukranu|sperm|night\s*fall|nightfall|swapnadosh|dhat|napunsak|mardana|marad|stamina\s*sex|sex\s*karta|sambhog)\b/i.test(_said);

    var _femaleContext = /\b(period|periods|mahwari|mahavari|pcod|pcos|pregnan\w*|garbh\w*|conceiv\w*|breast\s*feed\w*|breastfeed\w*|menopaus\w*|ovary|ovaries|bacchedani|uterus|delivery\s*hui)\b/i.test(_said);
    if (_maleConcern && !_femaleContext) {
      pregnancyGuard = true;
      aiText = 'Theek hai, note kar liya. Bas ye bata dijiye - koi health problem hai ya koi dawai chal rahi hai? Uske hisab se main sahi guidance de deti hu.';
      console.log('[Pregnancy Guard] BLOCKED a pregnancy/breastfeeding question on a male concern'
        + ' | was=' + JSON.stringify(String(aiText).slice(0, 90)));
    }
  }
}

let safetyClaimGuard = false;
const _SAFETY_CLAIM_RE = new RegExp(
     '\\b(side\\s*effects?|nuksan|nuksaan|haani)\\b[^.!?]{0,24}\\b(nahi|nhi|nai)\\b'
  + '|\\bno\\s+side\\s*effects?\\b'
  + '|\\b100\\s*(%|percent|pratishat)\\s*(safe|sure|cure|guaranteed?|result)\\b'
  + '|\\b(bilkul|puri\\s*tarah|puri\\s*trah|totally|completely)\\s*safe\\b'
  + '|\\b(guarantee|guaranteed)\\b[^.!?]{0,20}\\b(result|cure|ilaj|theek|thik)\\b', 'i');

const _claimInReply = _SAFETY_CLAIM_RE.test(String(aiText));
const _claimByModel = _SAFETY_CLAIM_RE.test(_modelText);
if (_claimInReply || _claimByModel) {
  const _blocked = (_claimInReply ? String(aiText) : _modelText).slice(0, 80);
  safetyClaimGuard = _claimInReply ? 'blocked' : 'model_only';
  if (_claimInReply) {
    aiText = 'Ji, kisi bhi product ki safety ya result ka dava main khud se nahi kar sakti - ye doctor hi aapki '
      + 'puri history dekh kar batate hain. Aap apna naam aur kaunsa time aapke liye theek rahega bata dijiye, '
      + 'main free consultation set kara deti hu.';
  }
  console.log('[Safety Claim Guard] ' + safetyClaimGuard + ' an absolute safety/cure claim | was='
    + JSON.stringify(_blocked));
}

if (!greetedBefore) {
  aiText = 'Hello ji, mai Ananya baat kar rahi hu SehatUP se. ' + (aiText || 'mai aapki kya help kar sakti hu?');
}
if (!aiText) aiText = 'bataiye, mai aapki kya help kar sakti hu?';

const JI_MAX = 1;
function qrTrimJi(t) {
  let kept = 0;
  return String(t || '')

    .replace(/\bji\b\s*,?\s*/gi, (m, off, s) => {
      if (++kept <= JI_MAX) return m;
      if (off + m.length >= s.length) return '';

      if (/(^|[.!?\n]\s*)$/.test(s.slice(0, off))) return '';
      return m.includes(',') ? ', ' : ' ';
    })
    .replace(/[ \t]{2,}/g, ' ')
    .replace(/\s+([,.!?])/g, '$1')
    .replace(/([,.!?])\s*\1+/g, '$1')
    .replace(/^[\s,]+/, '')
    .replace(/[ \t]+$/gm, '')

    .replace(/([.!?] +)([a-z])/g, (m, a, b) => a + b.toUpperCase())
    .trim();
}
aiText = qrTrimJi(aiText);
if (!aiText) aiText = 'bataiye, mai aapki kya help kar sakti hu?';

const _guardNames = ['roleGuard', 'scoreGuard', 'reportGuard', 'identityGuard', 'bookingGuard', 'substanceGuard',
  'repeatGuard', 'companyGuard', 'nameGuard', 'claimGuard', 'doseGuard', 'slotGuard', 'priceGuard', 'promiseGuard',
  'pitchGuard', 'safetyClaimGuard', 'consultModeGuard', 'inventGuard', 'aiIdentityGuard', 'bookingClaimGuard', 'pregnancyGuard'];
const _flagVals = { roleGuard, scoreGuard, reportGuard, identityGuard, bookingGuard, substanceGuard,
  repeatGuard, companyGuard, nameGuard, claimGuard, doseGuard, slotGuard, priceGuard, promiseGuard,
  pitchGuard, safetyClaimGuard, consultModeGuard, inventGuard, aiIdentityGuard, bookingClaimGuard, pregnancyGuard };
const _flags = _guardNames.filter(k => _flagVals[k])
  .map(k => k.replace(/Guard$/, '') + (typeof _flagVals[k] === 'string' ? '=' + _flagVals[k] : ''));
if (emptyReason) _flags.push('empty=' + emptyReason);
const aiGuards = _flags.join(',');
try {
  $execution.customData.set('reply', String(aiText).slice(0, 60));
  $execution.customData.set('guards', aiGuards || 'none');
} catch (e) {  }
console.log('[Extract AI Response] guards=' + (aiGuards || 'none') + ' | reply=' + JSON.stringify(String(aiText).slice(0, 60)));

const docId = phone.replace(/[^0-9]/g, '');
return [{
  json: {
    phone,
    name,
    lastAiReply: aiText,
    aiGuards,
    lastUserMessage: newMsgText,
    docId,
    roleGuard,
    scoreGuard,
    reportGuard,
    identityGuard,
    bookingGuard,
    substanceGuard,
    repeatGuard,
    companyGuard,
    nameGuard,
    claimGuard,
    doseGuard,
    slotGuard,
    priceGuard,
    promiseGuard,
    pitchGuard,
    safetyClaimGuard,
    consultModeGuard,
    inventGuard,
    aiIdentityGuard,
    bookingClaimGuard,
    pregnancyGuard,
    emptyReason,
    lastUpdated: new Date().toISOString()
  }
}];
