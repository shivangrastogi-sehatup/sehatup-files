# --- Fine-tuning stack (pinned to a known-good, mutually compatible set) ---
# NOTE: install torch SEPARATELY first, matched to your CUDA version, e.g.
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
# Then: pip install -r requirements.txt

transformers==4.46.3
trl==0.12.2
peft==0.13.2
datasets==3.1.0
accelerate==1.1.1
bitsandbytes==0.44.1     # 4-bit QLoRA. Windows wheels exist; on Linux/Colab it "just works".

# --- Experiment tracking ---
mlflow>=2.17,<3

# --- misc ---
pyyaml>=6.0
pandas>=2.0
